{
  "actorOutputSchemaVersion": 1,
  "title": "Refinery cleaned text",
  "description": "Dataset items with text, word_count, language, and processing_time_ms per page.",
  "properties": {
    "results": {
      "type": "string",
      "title": "Cleaned pages (dataset)",
      "template": "{{links.apiDefaultDatasetUrl}}/items"
    }
  }
}
